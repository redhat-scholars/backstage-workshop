(function (window) {
  window["env"] = window["env"] || {};
  window["env"]["mapTitle"] = "${MAP_TITLE}";
  window["env"]["mapAttribution"] = "${MAP_ATTRIBUTION}";
  window["env"]["mapUrl"] = "${MAP_URL}";
  window["env"]["mapToken"] = "${MAP_TOKEN}";
  window["env"]["gatewayApiUrl"] = "${GATEWAY_API_URL}";
  window["env"]["websocketEndpoint"] = "${WEBSOCKET_ENDPOINT}";
})(this);
